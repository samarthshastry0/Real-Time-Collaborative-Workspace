# JWT Configuration
# Generate a strong random secret key (at least 256 bits / 32 characters)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-this-to-a-random-string

# Database Configuration
DB_URL=jdbc:postgresql://localhost:5433/workspace_db
DB_USERNAME=workspace_user
DB_PASSWORD=workspace_password
